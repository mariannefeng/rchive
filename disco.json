{
  "version": "1.0",
  "services": {
    "web": {
      "port": 8000,
      "health": {
          "command": "curl -f http://example.com || exit 1"
      },
      "volumes": [
        {
          "name": "data",
          "destinationPath": "/app/volume"
        }
      ]
    }
  }
}
